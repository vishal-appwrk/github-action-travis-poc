language: node_js
node_js:
    - "stable"
before_script:
    - npm install 
script:
    - npm run build  
# safelist
branches:
    only:
    - master 
cache:
  directories:
  - node_modules
# before_script:
  #- npm install -g gulp-cli
#script:
 # - npm test
 
 # - npm run build

  # Added the test comment on develop branch
before_deploy: "echo 'I am ready to deploy to S3 Bucket'"
deploy:
    provider: s3
    access_key_id: $access_key_id
    secret_access_key: $secret_access_key
    bucket: "cdn.fusioncharts.com"
    skip_cleanup: true
    local_dir: build
    #upload-dir: fusioncharts/fc-licensing
    region: eu-central-1
    on:
      branch: master
after_deploy: "echo 'I done with the deploy to S3 Bucket. Please check the Logs'"
